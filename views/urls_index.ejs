<!DOCTYPE html>
<html lang="en">
<head>
    <% include ./partials/_header %>
    <title>TinyApp</title>
</head>
    <body>
      <% for (let key in urls) { %>
      <table style="width:25%">
        <tr>
          <td><%= key %></td>
          <td><%= urls[key] %></td>
          <% } %>
        </tr>
      </table>
    <a href='#' </a>
    </body>
</html>